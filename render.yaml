services:
  - type: web
    name: cashflowgo-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn cashflowgo.wsgi
    envVars:
      - key: DJANGO_DEBUG
        value: "false"
      - key: DJANGO_SECRET_KEY
        sync: false
